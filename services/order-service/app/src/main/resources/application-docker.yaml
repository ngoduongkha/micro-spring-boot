spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://postgres:5432/product-service
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

server:
  port: 10500

logging:
  level:
    web: DEBUG