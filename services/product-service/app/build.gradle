plugins {
    id("io.micro-spring-boot.spring-boot-application")
}

group = "${group}.product-service"

dependencies {
    implementation(project(":domain"))
    implementation(project(":infra"))

    implementation("io.micro-spring-boot.building-blocks:jpa")
}
